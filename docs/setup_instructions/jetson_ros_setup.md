# Jetson ROS Setup

Instructions for setting up the ROS environment after setting up the Jetson environment.

## Scripts

Scripts should be put in their respective packages in the Catkin workspace.

The weights for each model should be all stored in one folder in the home directory named "Model_Weights" In this repository, the model weights directory can be found under the scripts subfolder.
